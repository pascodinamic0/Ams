# 2026-08-25

## Reminder: Twilio + WhatsApp phone auth (due ~2026-09-01)

Pascal paused Twilio billing — no $20 starting balance yet.

**Goal status:** PAUSED on Twilio payment. Code + free prep done. Live WhatsApp OTP blocked until ~Sep 1.

### Free now (no payment)
- [ ] Apply [Twilio for Startups](https://www.twilio.com/startups) (Digni Digital LLC / ShuleOS)
- [ ] Start Meta WABA application (business.facebook.com)
- [ ] Supabase Dashboard ? Auth ? Phone ? enable provider (Twilio creds optional until funded)
- [x] `bun run seed:phone-auth-test` — linked `+243822000001` to `parent@ams.demo` on hosted
- [ ] Local demo: install Supabase CLI ? `supabase start` ? `/login` Phone ? +243822000001 ? OTP 123456

See `docs/PHONE_WHATSAPP_AUTH.md` section **0. Free setup**.

### When funded (~Sep 1)
1. Twilio Pay as you go — $20 minimum (DRC has no free trial)
2. Create Verify Service (`VA…`), link WhatsApp Sender on Verify ? WhatsApp tab
3. Supabase ? Auth ? Phone ? Twilio Verify, OTP expiry 300s, phone signup OFF
4. Set `[auth.sms.twilio_verify] enabled = true` in config.toml + Twilio env vars
5. Test `/login` ? Phone tab with real WhatsApp delivery

**Code in repo:** phone login UI, settings phone link, `scripts/seed-phone-auth-test.mjs`

**Blocked on:** Twilio $20 + Meta WABA verification
